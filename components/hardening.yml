name: Hardening
schemaVersion: 1.0
phases:
  - name: build
    steps:
      - name: Harden
        action: ExecutePowerShell
        inputs:
          commands:
            - Set-ExecutionPolicy RemoteSigned -Force
