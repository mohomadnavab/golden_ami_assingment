name: Patch
schemaVersion: 1.0
phases:
  - name: build
    steps:
      - name: Update
        action: ExecutePowerShell
        inputs:
          commands:
            - Install-WindowsUpdate -AcceptAll -AutoReboot
